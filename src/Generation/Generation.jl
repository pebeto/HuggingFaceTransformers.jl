"""
    Allspark.Generation

Decoder sampling: KV-cache type, `generate()` (greedy / temperature /
top-k / top-p / repetition penalty / max_new_tokens / EOS), and the minimal
Jinja subset needed to render chat templates from `tokenizer_config.json`.
"""
module Generation

end # module Generation
